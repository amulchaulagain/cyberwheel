# Evaluation Parameters
red_agent: rl_red_agent.yaml
blue_agent: inactive_blue_agent.yaml
environment: CyberwheelRedRL
valid_targets: all
train_red: true
train_blue: false
campaign: false
seed: 1

checkpoint: agent
download_model: false
run: # required if download_model is true
wandb_project_name: # required if download_model is true
wandb_entity: # required if download_model is true

experiment: training_red_evaluator
visualize: false
graph_name:
network_config: 15-host-network.yaml
decoy_config: decoy_hosts.yaml
host_config: host_defs_services.yaml
detector_config: detector_handler.yaml
min_decoys: 2
max_decoys: 3
reward_scaling: 10.0
reward_function: RLRedReward
num_steps: 50
num_episodes: 10
